apiVersion: v1
kind: Service
metadata:
  name: repl-rust-api
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: repl-rust-api
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: repl-rust-api
spec:
  selector:
    matchLabels:
      app: repl-rust-api
  replicas: 1
  template:
    metadata:
      labels:
        app: repl-rust-api
    spec:
      containers:
        - name: repl-rust-api
          image: mtrqq/repl-backend:0.0.7-rust
          ports:
            - containerPort: 80
